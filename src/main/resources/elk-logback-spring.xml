<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty name="LOGSTASH_HOST" source="LOGSTASH_HOST" defaultValue="localhost"/>
    <springProperty name="LOGSTASH_PORT" source="LOGSTASH_PORT" defaultValue="5000"/>

    <!-- Консольный appender (для локальной отладки) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logstash TCP appender -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <!-- если логируем в докер, используем имя сервиса logstash -->
        <destination>${LOGSTASH_HOST}:${LOGSTASH_PORT}</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- включаем MDC и исключения в структуру -->
            <includeMdcKeyName>requestId,userId</includeMdcKeyName>
        </encoder>
        <reconnectionDelay>5000</reconnectionDelay>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOGSTASH"/>
    </root>
</configuration>
